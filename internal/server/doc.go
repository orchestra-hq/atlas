// Package server is the control plane: gateway (auth, routing, SSE),
// worker registry, scheduler (trivial in M0), and the hub workers dial
// into (ADR-0003).
//
// Populated from phase 2 of docs/m0-build-plan.md.
package server
